# Полное руководство по установке MUSE для новичков

## Введение

Добро пожаловать! Это пошаговое руководство поможет вам установить и настроить MUSE (Memory-Utilizing and Self-Evolving Agent), даже если вы никогда раньше этого не делали и не зарегистрированы ни в каких сервисах.

MUSE - это интеллектуальный агент, который может выполнять сложные задачи, используя память и способность к самообучению. Он может работать с веб-сайтами, обрабатывать информацию и выполнять задачи на вашем компьютере.

## Содержание

1. [Подготовка к установке](#подготовка-к-установке)
2. [Установка необходимых программ](#установка-необходимых-программ)
3. [Регистрация аккаунтов](#регистрация-аккаунтов)
4. [Скачивание и установка MUSE](#скачивание-и-установка-muse)
5. [Настройка MUSE](#настройка-muse)
6. [Тестирование установки](#тестирование-установки)
7. [Частые ошибки и их решение](#частые-ошибки-и-их-решение)

## Подготовка к установке

### Что вы узнаете из этого руководства
- Как установить все необходимые программы для MUSE
- Как зарегистрироваться в нужных сервисах
- Как настроить MUSE под свои нужды
- Как проверить, что все работает правильно

### Требования к системе
- Компьютер с операционной системой Windows, macOS или Linux
- Подключение к интернету
- Не менее 4 ГБ свободного места на диске
- Администраторские права на компьютере (для установки программ)
- 8 ГБ оперативной памяти (рекомендуется)

## Установка необходимых программ

### Шаг 1: Установка Python

Python - это язык программирования, необходимый для работы MUSE.

#### Для Windows:
1. Перейдите на сайт https://www.python.org/
2. Нажмите на зеленую кнопку "Download Python" (Скачать Python)
3. Скачайте установочный файл
4. Запустите скачанный файл
5. **Очень важно**: Поставьте галочку "Add Python to PATH" перед тем как нажать "Install Now"
6. Нажмите "Install Now" и дождитесь завершения установки

#### Для macOS:
1. Откройте Терминал (найдите в поиске Spotlight)
2. Установите Homebrew, если он еще не установлен:
   ```
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   ```
3. Установите Python:
   ```
   brew install python
   ```

#### Для Linux (Ubuntu/Debian):
1. Откройте Терминал
2. Выполните команды:
   ```
   sudo apt update
   sudo apt install python3 python3-pip
   ```

### Шаг 2: Установка Git

Git нужен для скачивания MUSE с интернета.

#### Для Windows:
1. Перейдите на сайт https://git-scm.com/
2. Нажмите "Download"
3. Запустите установочный файл
4. Нажимайте "Next" до конца установки, принимая все стандартные настройки

#### Для macOS:
Git уже установлен, если вы использовали Homebrew в предыдущем шаге.

#### Для Linux:
Git уже установлен, если вы выполнили команды в предыдущем шаге.

### Шаг 3: Установка Conda (рекомендуется)

Conda поможет изолировать зависимости MUSE от других программ:

#### Для Windows:
1. Скачайте Miniconda с https://docs.conda.io/en/latest/miniconda.html
2. Запустите установочный файл и следуйте инструкциям

#### Для macOS:
```
curl -sL https://repo.anaconda.com/miniconda/Miniconda3-latest-MacOSX-x86_64.sh > miniconda.sh
bash miniconda.sh
```

#### Для Linux:
```
curl -sL https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh > miniconda.sh
bash miniconda.sh
```

## Регистрация аккаунтов

Для работы MUSE требуются API-ключи от различных сервисов ИИ. Вот как получить их:

### GitHub (для скачивания кода MUSE):
1. Перейдите на сайт https://github.com/
2. Придумайте имя пользователя (username)
3. Введите ваш email
4. Придумайте надежный пароль
5. Нажмите "Sign up"
6. Подтвердите email, который придет на вашу почту

### Google AI Studio (для Gemini API):
1. Перейдите на сайт https://aistudio.google.com/
2. Нажмите "Get API Key" или "Sign in"
3. Войдите с вашим Google аккаунтом
4. Нажмите "Create API Key" или "Generate API Key"
5. Скопируйте и сохраните ключ в безопасном месте

### OpenAI (для GPT API):
1. Перейдите на сайт https://platform.openai.com/
2. Нажмите "Sign up" или "Log in" если у вас уже есть аккаунт
3. Перейдите в раздел "API Keys" в настройках
4. Нажмите "Create new secret key" или "Create API key"
5. Скопируйте и сохраните ключ в безопасном месте

### Другие сервисы ИИ (если требуются):
В зависимости от используемых моделей могут потребоваться ключи от других сервисов:
- Anthropic (для Claude): https://www.anthropic.com/
- Alibaba Cloud (для Qwen): https://www.alibabacloud.com/
- DeepSeek: https://www.deepseek.com/

Для каждого сервиса:
1. Создайте аккаунт или войдите в существующий
2. Найдите раздел "API Keys" или "Developer"
3. Создайте новый API-ключ
4. Сохраните ключ в безопасном месте

## Скачивание и установка MUSE

### Шаг 1: Скачивание кода MUSE

Откройте Терминал или командную строку:

1. Перейдите в папку, где вы хотите установить MUSE:
   ```
   cd /путь/к/папке
   ```
   или для Windows:
   ```
   cd C:\Users\ВашеИмя\Documents
   ```

2. Скачайте код MUSE с GitHub:
   ```
   git clone https://github.com/KnowledgeXLab/MUSE.git
   ```

3. Перейдите в папку с MUSE:
   ```
   cd MUSE
   ```

### Шаг 2: Установка зависимостей

1. Убедитесь, что вы находитесь в папке с MUSE
2. Создайте виртуальное окружение с помощью conda:
   ```
   conda create -n MUSE python=3.12
   conda activate MUSE
   ```
   или если вы не используете conda:
   ```
   python -m venv muse_env
   source muse_env/bin/activate  # На Windows: muse_env\Scripts\activate
   ```

3. Установите все необходимые библиотеки:
   ```
   pip install -r requirements.txt
   ```

4. Установите дополнительные зависимости для браузера:
   ```
   playwright install chromium
   playwright install-deps chromium
   ```

## Настройка MUSE

### Шаг 1: Создание файла конфигурации

1. Найдите файл `config.yaml` в папке с MUSE
2. Откройте его в любом текстовом редакторе (например, Notepad на Windows)
3. В этом файле находятся настройки для различных моделей ИИ, таких как Gemini, GPT, Claude и другие
4. Вам нужно будет настроить переменные окружения для API-ключей

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в главной папке MUSE:

1. В папке MUSE создайте новый текстовый файл и назовите его `.env`
2. Добавьте в него следующие строки:
   ```
   API_KEY=ваш_API_ключ_здесь
   BASE_URL=https://generativelanguage.googleapis.com/v1beta  # для Gemini
   ```
3. Замените `ваш_API_ключ_здесь` на реальный API-ключ, который вы получили ранее
4. Для использования других моделей (например, GPT) измените BASE_URL при необходимости

### Шаг 3: Выбор модели ИИ

В файле `config.yaml` вы можете выбрать, какую модель ИИ использовать:

- Для Gemini: используйте `gemini-2.5-flash` или `gemini-2.5-pro`
- Для GPT: используйте `gpt-4.1` или другие модели OpenAI
- Для Claude: используйте `claude` или `claude-thinking`

Чтобы выбрать модель, убедитесь, что у вас есть соответствующий API-ключ и правильная настройка BASE_URL в файле `.env`.

## Тестирование установки

### Шаг 1: Проверка установки зависимостей

Запустите команду для проверки, что все зависимости установлены правильно:
```
python -c "import sys; print(sys.version)"
```

Затем проверьте, что основные библиотеки MUSE работают:
```
python -c "import yaml, openai, pydantic"
```

### Шаг 2: Запуск демонстрации MUSE

1. Убедитесь, что вы находитесь в папке с MUSE и виртуальное окружение активировано
2. Запустите демонстрационный скрипт:
   ```
   python demo.py
   ```

### Шаг 3: Проверка функциональности

После запуска демонстрации, MUSE должен:
- Подключиться к выбранной модели ИИ
- Принять от вас задание
- Попытаться выполнить его, используя браузер и другие инструменты
- Сообщить о результатах выполнения задания

Если вы видите сообщения об ошибках, проверьте:
- Правильно ли настроены API-ключи
- Активировано ли виртуальное окружение
- Установлены ли все зависимости

## Частые ошибки и их решение

### Ошибка: "python не найден" или "command not found"
- Убедитесь, что Python установлен и добавлен в PATH
- Попробуйте использовать `python3` вместо `python` на macOS/Linux

### Ошибка: "permission denied"
- Запустите команду с `sudo` на macOS/Linux:
  ```
  sudo python demo.py
  ```
- Убедитесь, что у вас есть права администратора на Windows

### Ошибка: "module not found"
- Убедитесь, что вы установили все зависимости:
  ```
  pip install -r requirements.txt
  ```
- Проверьте, активировано ли виртуальное окружение

### Ошибка: "git не найден"
- Убедитесь, что Git установлен правильно
- Перезапустите Терминал после установки Git

### Проблемы с API-ключами
- Проверьте, что ключи введены правильно без лишних пробелов в файле `.env`
- Убедитесь, что учетная запись с API-ключом активна
- Проверьте, не истек ли срок действия ключа
- Убедитесь, что BASE_URL в файле `.env` соответствует выбранной модели ИИ

### Ошибки браузера Playwright
- Если возникают ошибки с браузером, убедитесь, что установили Playwright правильно:
  ```
  playwright install chromium
  playwright install-deps chromium
  ```
- На некоторых системах может потребоваться установить дополнительные зависимости

### Ошибки при запуске MUSE
- Убедитесь, что вы запускаете MUSE из правильной директории
- Проверьте, что виртуальное окружение активировано
- Убедитесь, что файлы `config.yaml` и `.env` находятся в правильной папке
- Проверьте, что у вас есть подключение к интернету

## Дополнительная помощь

Если у вас возникли проблемы с установкой:

1. **Проверьте официальную документацию** - посетите репозиторий GitHub MUSE: https://github.com/KnowledgeXLab/MUSE
2. **Поищите решения в интернете** - скопируйте текст ошибки и найдите решения в Google
3. **Обратитесь за помощью** - создайте "Issue" на странице GitHub программы
4. **Попросите помощи у знакомых** - возможно, кто-то уже сталкивался с этой проблемой

## Полезные советы

- **Сохраняйте API-ключи в безопасном месте** - не делитесь ими с другими
- **Делайте резервные копии** - сохраняйте важные файлы конфигурации
- **Читайте сообщения об ошибках** - они часто содержат подсказки к решению
- **Не бойтесь экспериментировать** - большинство ошибок можно исправить
- **Записывайте свои действия** - это поможет в случае возникновения проблем
- **Используйте виртуальные окружения** - это поможет избежать конфликта зависимостей
- **Следите за обновлениями** - проекты ИИ быстро развиваются, регулярно проверяйте наличие обновлений

## Заключение

Поздравляем! Вы успешно установили MUSE. Теперь вы можете начать использовать интеллектуального агента для выполнения различных задач. MUSE может работать с веб-сайтами, обрабатывать информацию и выполнять сложные задания, используя память и способность к самообучению.

Не забывайте периодически обновлять MUSE до последней версии, чтобы получать новые функции и исправления ошибок:
```
git pull
```

Затем снова установите зависимости, если были обновления:
```
pip install -r requirements.txt
```

Для получения более подробной информации о возможностях MUSE и участии в бенчмарке TAC (The Agent Company Benchmark), посетите:
👉 [TheAgentCompanyForMuse Repository](https://github.com/KnowledgeXLab/TheAgentCompanyForMuse)

Удачи в использовании MUSE!